#!/bin/bash
# Pre-commit hook - solo lint rápido para todos los commits

echo "🧹 Running lint check..."
npm run lint

if [ $? -ne 0 ]; then
    echo "❌ Linter failed! Fix errors before committing."
    exit 1
fi

echo "✅ Lint check passed!"
